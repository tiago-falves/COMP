import static io.println(int);
class Conditionals {
	public static void main(String[] args) {
        int a;
        int b;        
        int i;
        int j;
        int[] newField;
        Conditionals x;

        a = 4;
        b = 1;

        x = new Conditionals();
        
        i = 0;
        newField = new int[5];
        while(i < newField.length){
            newField[i] = newField.length - i;
            i = i + 1;
            j = i;
            while(j < newField.length){
                j = j + 1;
                io.println(j+i*j);
            }
        }

        a = x.add(1,1) * newField[1] + (2 * newField[1]);
        
        b = new Conditionals().add(1,1);
        
        if((b+1) < (a-1)){
            io.println(1);
            if(true && false){
                // false
                io.println(0-1);
            }else{
                io.println(2);
                if(true && !false){
                    io.println(1+2);
                }else{
                    io.println(5);
                }
            }
            a = 0-1;
        }
        else{
            a = 4;
        }   

        io.println(a);
	}

	public int add(int a, int b){
		return a+b;
	}
}

